version: '3'
services:
  builder:
    image: ubuntu:22.04
    volumes:
      - .:/app
    working_dir: /app
    command: >
      /bin/bash -c "
        apt-get update &&
        apt-get install -y build-essential cmake &&
        mkdir -p build &&
        cd build &&
        cmake .. &&
        make"